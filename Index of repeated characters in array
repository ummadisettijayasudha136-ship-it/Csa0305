#include <stdio.h>
int main(){
    int arr[]={1,2,3,2,4,3},n=6;
    for(int i=0;i<n;i++)
        for(int j=i+1;j<n;j++)
            if(arr[i]==arr[j])
                printf("Value %d repeated at index %d and %d\n",arr[i],i,j);
    return 0;
}
