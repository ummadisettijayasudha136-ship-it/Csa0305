#include <stdio.h>
int main(){
    int arr[]={1,2,4,5},n=4,sum=0;
    for(int i=0;i<n;i++) sum+=arr[i];
    int expected=(n+1)*(n+2)/2;
    printf("Missing element = %d\n",expected-sum);
    return 0;
}
