#include <stdio.h>
int main(){
    int a[3][3]={{1,5,3},{9,2,6},{4,7,8}},max=a[0][0];
    for(int i=0;i<3;i++)
        for(int j=0;j<3;j++)
            if(a[i][j]>max) max=a[i][j];
    printf("Largest element = %d\n",max);
    return 0;
}

